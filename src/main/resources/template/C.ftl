/**
 * ${id}. ${title}
 * https://leetcode.cn/problems/${titleSlug}/
 */

#include <stdio.h>
#include <stdlib.h>
 <#list includeList as include>
#include ${include}
</#list>

${cCode}

int main() {
    return 0;
}